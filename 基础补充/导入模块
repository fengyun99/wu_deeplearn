1.想要导入模块直接使用import导入，import 包名.模块名
2.想要通过from 包名 import 模块名，则是需要在包下创建__init__.py文件，并且写入: __all__ = ['模块名1','模块名2']，导入包时就会执行__init__.py文件

如果一个文件中有all变量，那么也就意味着这个变量中的元素，不会被from xxx import *时导入,不写入__all__就无法被*导入

测试模块，在模块中使用 if __name__ == 'main': 进行测试

